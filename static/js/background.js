/*
   LUNASHID BACKGROUND
   Background is controlled by Flask/Jinja.
   This file intentionally does not overwrite it.
*/

document.addEventListener("DOMContentLoaded", () => {
    document.documentElement.classList.add("background-ready");
});
